# tidyverse

## install

```R
// install package to system
install.packages("tidyverse")

// load
library(tidyverse)

// update
tidyverse_update()

```

## memo 
```R
// r4dsで使うpackage
install.packages(c("nycflights13", "gapminder", "Lahman"))
```

## tips 

* `dput()`でdumpできる。
